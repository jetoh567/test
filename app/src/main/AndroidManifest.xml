<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gurufinalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permisson.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MemberMainActivity"></activity>
        <activity android:name=".activity.MyNoteViewActivity" />
        <activity android:name=".activity.NoteWriteActivity" />
        <activity android:name=".activity.NoteDetailActivity" />
        <activity android:name=".activity.AdminJoinActivity" />
        <activity android:name=".activity.MemberJoinActivity" />
        <activity android:name=".activity.SelectLoginActivity" />
        <activity android:name=".activity.MemberDetailActivity" />
        <activity android:name=".activity.NotifyActivity" />
        <activity android:name=".activity.administrator.AdministratorMainActivity" />
        <activity android:name=".activity.administrator.CheckCommunalPropertyActivity" />
        <activity android:name=".activity.administrator.CheckLostArticleActivity" />
        <activity android:name=".activity.administrator.CheckOutsiderActivity" />
        <activity android:name=".activity.administrator.CheckWildAnimalActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>